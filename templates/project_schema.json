{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Project Initialisation Metadata (Payload)",
    "description": "Hyperparameter fields required of a partipant in PySyft FL training",
    "type": "object",
    "properties": {
        
        "project_id": {
            "description": "Unique Identifiers for a project",
            "type": "string"
        },

        "participants": {
            "description": "Participants registered for FL training under this project",
            "type": "array",
            "items": {
                "description": "Participant ID",
                "type": "string"
            }

        },

        "experiments": {
            "description": "Experiments registered for FL training under this project",
            "type": "array",
            "items": {
                "description": "Experiment ID",
                "type": "string"
            }
        },

        "incentives": {
            "description": "Experiments registered for FL training under this project",
            "type": "object"
        },

        "created_at": {
            "description": "Date at which project was created",
            "type": "string"
        }
    },

    "required": ["project_id"]
}